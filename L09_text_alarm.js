let xpos;
function setup() {
    createCanvas(600,400);
    background(200);
xpos = width/2
}

function draw() {
    textSize(10);
    textAlign(CENTER,CENTER);
    text("the yoinky sploinky testing v1", width/2,height/2);
}