let starwars;

function preload() {
    starwars = loadSound('assets/star_wars_theme_8_bit.mp3');
}

function setup() {
    createCanvas(400,400);
    background(200);
}

function draw() {
    if ( keyIsDown(32)) {
        star
    }
}